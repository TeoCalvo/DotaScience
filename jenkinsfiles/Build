pipeline {

    agent any

    stages {

        stage("Build Image"){
            steps{
                sh "docker build -t dota_img ."
            }
        }
       
        stage("Container up"){
            steps{
                sh "docker run -t dota_con dota_img"
            }
        }

    }
}