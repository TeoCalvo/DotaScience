pipeline {

    agent any

    stages {

        stage("Build"){
            steps{
                sh "docker build -t dota_img ."
            }
        }

        stage("Up"){
            steps{
                sh "docker run --name dota_con dota_img"
            }
        }
    }
}