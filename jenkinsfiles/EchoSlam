pipeline {
    agent any

    stages{

        stage("Exec"){
            steps{
                sh "docker exec python-dota python root/dotaScience/echos_slam/exec.py"
            }
        }

    }
}